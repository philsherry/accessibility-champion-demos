node_modules/
test-results/
playwright-report/
blob-report/
package-lock.json

# djlint already formats these (including embedded <style>/<script> blocks
# via format_css/format_js in .djlintrc) — Prettier would just fight it.
*.html

# Markdown formatting/wrapping is out of scope for this lint-tooling pass —
# not touched by this project's linters otherwise.
*.md
