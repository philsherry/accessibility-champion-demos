# TODO

Tracks the remaining work from the book repo's "More Nip & Claw" TODO item (`accessibility-champion/TODO.md`). Automated test coverage (Playwright + axe-core, PR #1) and the manual-review follow-up fixes (mobile header tab-order, tagline, skip-link focus, table headers, PR #3) are both done and merged to `main` — not repeated here.

- [x] **Build the sketched extensions** — `admin.html` (staff order management) and `receipt.html` (order receipt, reached from checkout). Design: `docs/superpowers/specs/2026-07-11-admin-and-receipt-pages-design.md`. Plan: `docs/superpowers/plans/2026-07-11-admin-and-receipt-pages.md`. `store.html` (retail store locator) and per-order historical receipt links remain unbuilt — see the design doc's "Out of scope."
- [ ] **Track down intermittent test failures under full `npm test` runs** — a real, reproducible reliability gap, not incidental flakiness. Blocks treating `feat/offline-caching` as finished; the bar is a genuinely repeatable green run, not "passes most of the time." **Up next.**
  - **Fixed already**: `tests/service-worker.spec.ts` was racing all 3 Mobile/Tablet/Desktop *projects'* full-site precache bursts against the one dev `http-server` at once (worse once `admin.html`/`receipt.html` grew `PRECACHE_URLS`). Restricted the whole file to the `Desktop` project only via `test.skip()` in `beforeEach` (service worker behaviour isn't viewport-dependent) — verified 0 SW failures / 10 correctly-skipped across 3 repeat `npm test` runs.
  - **Also cleaned up**: 3 orphaned `http-server` processes from earlier ad-hoc debug servers (their `process.kill(-server.pid)` calls had silently failed with `ESRCH`), left running for over an hour and adding to dev-server contention. Killed by specifically-verified PID (`ps -p <pid> -o pid,ppid,etime,command`), not a broad `pkill`.
  - **Still open**: even after both fixes, roughly 2 of every 3 full `npm test` runs show one intermittent failure — a different file/page each time (seen so far: `footer-site.spec.ts` "links to the staff admin view" for checkout.html on Mobile; `responsive-layout.spec.ts` "header switches to the stacked mobile layout" for offline.html/accessibility.html on Mobile or Desktop). Re-running just the failing files in isolation once surfaced a more severe `browser.newContext: Target page, context or browser has been closed`. Same symptom class as the service-worker case — dev-server/browser contention under full concurrent load — just no longer masked by the SW tests eating most of the capacity.
  - **Working hypothesis, not yet applied**: `playwright.config.ts` has `fullyParallel: true` (line 5) and no explicit `workers` cap. The 3 `projects` (Mobile/Tablet/Desktop) run concurrently with each other regardless of any single file's own serial mode, and this machine has 6 CPU cores (`sysctl -n hw.ncpu`) — that combination can oversubscribe the machine relative to what the single-threaded dev `http-server` and the Chromium instances themselves can reliably service.
  - **Next step**: try an explicit, conservative `workers` cap in `playwright.config.ts`, then re-verify with several repeated full `npm test` runs in a row — not a single pass — before calling this resolved.
- [ ] **Style the admin status `<select>` properly** — `.status-select` in `public/assets/css/admin.css` has the baseline Customizable Select work (colour-coded options via `@supports (appearance: base-select)`, native fallback, real text label kept on every option). Flagged as not actually done yet — needs another pass against:
  - https://webkit.org/blog/18117/the-golden-rule-of-customizable-select/
  - https://lucide.dev/icons/?search=delivery — icon set for the option content, sitting alongside the existing colour and text, not replacing either.
- [ ] **Toast notification pattern review** — the "Add to cart" toast on `index.html` (bottom-centre, fixed position, `addToCart()` in `assets/js/index.js`) can disappear before a user notices it, and a screen magnifier user zoomed in elsewhere on the page can miss it entirely regardless of position. Screen reader users are already covered independently via the `#cart-status` live region — the toast is supplementary feedback for sighted users only. Not caught by axe-core (timing and magnifier-exclusion aren't automatable checks) — found by manual review. Needs its own look: whether the toast should persist until dismissed, be removable via `prefers-reduced-motion`-style user control, or whether a different pattern serves sighted users better here.
