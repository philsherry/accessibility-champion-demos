# TODO

Tracks the remaining work from the book repo's "More Nip & Claw" TODO item (`accessibility-champion/TODO.md`). Automated test coverage (Playwright + axe-core, PR #1) and the manual-review follow-up fixes (mobile header tab-order, tagline, skip-link focus, table headers, PR #3) are both done and merged to `main` — not repeated here.

- [ ] **Wider chapter usage** — reference existing demo pages from more book chapters (sections 8–12), selectively, not forced into every thread (e.g. probably doesn't fit poverty-focused chapters).
- [ ] **Build the sketched extensions** — `admin.html` staff order-management view and/or retail framing, already sketched in the book repo's `09-workplace-office/_SCRATCH.md` and `10-workplace-retail/_SCRATCH.md`. Needs its own brainstorm (scope, what it demonstrates, which chapter anchors it).
- [ ] **"Why we built this" appendix page** — a page telling the story of the demo: why it exists, what it covers, the pattern-to-chapter map, how it stays accessible. Write last, after other sub-items ship, so the story is real rather than aspirational.
- [ ] **Dark mode** — a solid accessibility boost, flagged as a future idea. Not scoped or brainstormed yet.
- [ ] **Toast notification pattern review** — the "Add to cart" toast on `index.html` (bottom-centre, fixed position, `addToCart()` in the inline `<script>`) can disappear before a user notices it, and a screen magnifier user zoomed in elsewhere on the page can miss it entirely regardless of position. Screen reader users are already covered independently via the `#cart-status` live region — the toast is supplementary feedback for sighted users only. Not caught by axe-core (timing and magnifier-exclusion aren't automatable checks) — found by manual review. Needs its own look: whether the toast should persist until dismissed, be removable via `prefers-reduced-motion`-style user control, or whether a different pattern serves sighted users better here.
