# TODO

Tracks the remaining work from the book repo's "More Nip & Claw" TODO item (`accessibility-champion/TODO.md`). Automated test coverage (Playwright + axe-core, PR #1) and the manual-review follow-up fixes (mobile header tab-order, tagline, skip-link focus, table headers, PR #3) are both done and merged to `main` — not repeated here.

- [x] **Build the sketched extensions** — `admin.html` (staff order management) and `receipt.html` (order receipt, reached from checkout). Design: `docs/superpowers/specs/2026-07-11-admin-and-receipt-pages-design.md`. Plan: `docs/superpowers/plans/2026-07-11-admin-and-receipt-pages.md`. `store.html` (retail store locator) and per-order historical receipt links remain unbuilt — see the design doc's "Out of scope."
- [x] **Track down intermittent test failures under full `npm test` runs** — a real, reproducible reliability gap, not incidental flakiness. Verified with 4 consecutive clean full `npm test` runs (635 passed / 0 failed each time), plus `npm run lint`.
  - **Fixed**: `tests/service-worker.spec.ts` was racing all 3 Mobile/Tablet/Desktop *projects'* full-site precache bursts against the one dev `http-server` at once (worse once `admin.html`/`receipt.html` grew `PRECACHE_URLS`). Restricted the whole file to the `Desktop` project only via `test.skip()` in `beforeEach` (service worker behaviour isn't viewport-dependent).
  - **Fixed — the real root cause of the SW test's own remaining failure**: `waitForPrecache()`'s use of `page.waitForFunction()` with an async predicate (reads the Cache API, itself async) intermittently returned after a single "not ready yet" evaluation instead of continuing to poll — reproduced deterministically running the SW spec alone with zero contention (no other projects, `workers: 1`), so this was never actually the contention bug it looked like from inside a full run. Confirmed by instrumenting the predicate and the service worker's install/activate/precache steps directly: the promise resolved while the cache was still empty, on both default `'raf'` polling and an explicit numeric `polling` interval — so the fix isn't a polling-mode tweak, it's not using `waitForFunction` for this predicate at all. Replaced with a plain `evaluate`-then-`sleep` loop driven from Node, which doesn't exhibit the problem (8/8 clean runs in isolation before rolling out).
  - **Also fixed**: `playwright.config.ts` had `fullyParallel: true` with no explicit `workers` cap, letting all 3 projects' tests hit the single dev `http-server` at once. Added `workers: process.env.CI ? 2 : 4` (machine has 10 cores, confirmed via `sysctl -n hw.ncpu` — the earlier "6 cores" note was a stale/incorrect reading). This addressed the separately-observed intermittent failures in `footer-site.spec.ts` and `responsive-layout.spec.ts` (not reproduced again across the 4 verification runs).
  - **Also cleaned up** (from an earlier pass): 3 orphaned `http-server` processes from ad-hoc debug servers, left running for over an hour and adding to dev-server contention. Killed by specifically-verified PID, not a broad `pkill`.
- [x] **Style the admin status `<select>` properly** — added a status icon (Lucide `package`/`truck`/`package-check`/`ban`, via one inline `<symbol>` sprite + `<use>` per option — no new asset files, no build step) alongside the existing colour and real text label in every `.status-select option`. Design: `docs/superpowers/specs/2026-07-11-admin-status-select-icons-design.md`. Plan: `docs/superpowers/plans/2026-07-11-admin-status-select-icons.md`. Checked directly against the WebKit golden-rule article (text stays primary, icon is `aria-hidden` decoration) — already satisfied, confirmed unchanged.
  - **Honest verification caveat**: `appearance: base-select` isn't active in any real-world browser checked by hand (Safari Technology Preview, latest Safari, Edge, Chrome Canary — including with `#enable-experimental-web-platform-features` manually enabled) — only in the Playwright-bundled "Chrome for Testing" build, which appears to pre-enable this for automated test authors. This was already true of the colour-coding *before* this change; the icon work inherits the existing gap rather than introducing a new one. The `@supports` guard means real visitors always get a fully functional plain-text `<select>` regardless — the enhancement activates automatically, no further code changes, whenever a browser ships default support. New test (`tests/components/admin.spec.ts`) verifies via `getComputedStyle()`/DOM structure against the one environment where this is actually checkable today.
- [x] **Toast notification pattern review** — researched the pattern in depth rather than just hardening the existing implementation: Adrian Roselli's "Defining 'Toast' Messages" (and its full update history through Nov 2025), GitHub Primer's production accessibility guidance (real moderated usability testing with NVDA/JAWS/VoiceOver/Windows Magnifier/Dragon users), Adam Silver's and TPGi's follow-ups. Consensus: toasts fail WCAG 2.2.1 Timing Adjustable without user-configurable duration, and are missed entirely by screen magnifier users — not theoretical, GitHub's own testing confirmed it, matching exactly what this item already flagged. Once "fixed" enough to be persistent/dismissible, it's no longer actually a toast.
  - **Resolution: removed the toast entirely**, not hardened. The header's cart-count badge already gave persistent, non-timed visual confirmation, and `#cart-status` already announced to screen reader users independently — the toast was pure redundancy once that was recognised. Removed the markup, the now-dead `addToCart()` wrapper in `assets/js/index.js` (`.product-cta` listeners call `addProductToCart` directly now, same as `orders.html`'s reorder buttons already did), and the CSS. New test in `tests/components/product-cards.spec.ts` asserts no `.toast`/`#toast` element exists and that the live region + badge still work.
  - **Also fixed along the way**: no `stylelint-order` plugin was installed, so property order within a CSS rule was never enforced — 269 violations existed across every CSS file once checked. Added the plugin with alphabetical ordering, auto-fixed the whole codebase (verified safe: no rule anywhere declares the same property twice, so reordering can't change which value wins; confirmed with the full test suite plus three repeat visual-regression runs, zero pixel differences).
